@model IEnumerable<EventStreamingPlatform.Models.Recomandation>

@{
    ViewData["Title"] = "Recomandation";
}

<h1>Recomandation</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Desc)
            </th>
     
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Desc)
                </td>
       
                <td>
                    <a asp-action="Edit" asp-route-id="@item.RecomandationId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.RecomandationId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.RecomandationId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>